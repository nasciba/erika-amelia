import styled from "styled-components";

export const PortfolioLink = styled.a`
  text-decoration: none;
  color: inherit;
  &:hover,
  &:focus,
  &:active,
  &:visited {
    color: inherit;
    text-decoration: none;
  }
`;
